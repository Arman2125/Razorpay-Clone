{
  "name": "mini-razorpay",
  "private": true,
  "version": "1.0.0",
  "description": "Mini Razorpay demo payment platform (MERN) - Razorpay AI Buildathon",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
